#include <bits/stdc++.h>

using namespace std;

int N;

void solve(){
    cin >> N;
    double k = N * 0.35 * 0.10;
    cout << (int)k;
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    solve();
    //cout << log2(1e9);
    return 0;
}
