#include <bits/stdc++.h>

using namespace std;

long long S, N;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> N;

    S = (N * N) + 1LL;

    cout << S;
    return 0;
}
